@using Admin.ViewModels
@model StatViewModel

@{
    ViewData["Title"] = "Stats";
}

<div class="container">
    @foreach (var type in Model.GetModuleKeys)
    {
        <div id="@type">
            <div class="panel panel-default">
                <div class="panel panel-title"><div style="padding: 5px">@Model.GetModuleNameByKey(type)</div></div>
                <table class="table">
                    <thead>
                        <tr>
                            @foreach (var sourceInfo in Model.GetModuleStatByKey(type))
                            {
                                <th>@sourceInfo.Name</th>
                            }
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            @foreach (var sourceInfo in Model.GetModuleStatByKey(type))
                            {
                                <td class="@(sourceInfo.IsDanger ? "danger" : "success")">@sourceInfo.Content</td>
                            }
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>
